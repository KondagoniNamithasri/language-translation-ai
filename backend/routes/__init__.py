"""
Route package for the backend.

Keeping this file intentionally minimal – it just marks the
`routes` directory as a Python package so imports like
`from routes.translation_route import translation_bp`
work when running `app.py` directly.
"""


